<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:screenOrientation="sensor"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Google sign-in redirect target. Google refuses OAuth from an embedded
                 WebView, so the flow runs in the system browser (Custom Tabs) instead;
                 this intent-filter is what lets Google's redirect hand control back to
                 the app instead of dead-ending in the browser. singleTask above means it
                 returns to this same running instance rather than a fresh one. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="games.whambam.app" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Speech recognition for the voice-guess mode -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <!-- Android 11 (API 30) package visibility. Without this the speech
         recogniser is invisible to the app: SpeechRecognizer.isRecognitionAvailable()
         returns false on a device that has one installed, and voice silently
         does nothing. -->
    <queries>
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
    </queries>

    <!-- Google Play Billing (RevenueCat in-app purchases) -->
    <uses-permission android:name="com.android.vending.BILLING" />

    <!-- Accelerometer drives the tilt engine, but the game is still playable
         with on-screen buttons, so this must not be a hard filter. -->
    <uses-feature android:name="android.hardware.sensor.accelerometer" android:required="false" />
</manifest>
