{
  "name": "headband-app",
  "version": "1.0.0",
  "description": "HeadBand! party game — native wrapper",
  "scripts": {
    "build": "echo 'No build step — using live URL'",
    "sync": "npx cap sync android",
    "open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/status-bar": "^6.0.0",
    "@capacitor/screen-orientation": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
