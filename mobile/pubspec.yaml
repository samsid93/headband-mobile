name: headband_mobile
description: A native mobile port of the HeadBand! party game.
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.1
  sensors_plus: ^5.0.1
  speech_to_text: ^6.6.0
  audioplayers: ^5.2.1
  shared_preferences: ^2.2.2
  google_fonts: ^6.1.0
  flutter_animate: ^4.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
  # Note: Assets and fonts would be defined here in a real project
